{
    "name": "hello-wasm",
    "version": "0.1.0",
    "dependencies": {
        "assemblyscript-json": "WebAssemblyOS/assemblyscript-json#near_dist",
        "near-runtime-ts": "github:nearprotocol/near-runtime-ts#staging"
    },
    "scripts": {
        "build": "mkdir -p ./out && near build && cp ./out/main.wasm ../hello.wasm",
        "build:as": "../../../bin/asc --runtime stub assembly/main.ts -o out/main.wasm --debug"
    },
    "devDependencies": {
        "bignum": "github:MaxGraey/bignum.wasm",
        "gulp": "^4.0.2",
        "near-shell": "github:nearprotocol/near-shell#staging"
    }
}
